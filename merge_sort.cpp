#include<iostream>
using namespace std;

void Merge(void)
{
    
}
main()
{
    //  merge sort algorithm
    //  q= midpoint, p= lower bound, r= upper bound
   
    //  Unsorted array declaration and Entering the value of array
   int siz;
    cout<<"Select the size of array "<<endl;
    cin>>siz;
    int arr[siz];
    cout<<"Enter the Element of array : "<<endl;
    for(int i=0; i<siz; i++)
    {
        cin>>arr[siz];
    }
    //  Variable declaration
    int p,q,r;
    
    //if condition for checking the spliting of array 
    if(p>=r)
    return

    

}